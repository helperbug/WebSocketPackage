import XCTest
@testable import WebSocketPackage

final class WebSocketPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(WebSocketPackage().text, "Hello, World!")
    }
}
